version: "3.7"
services:
  init-env:
    image: ntdtfr/env-initializer
    configs:
      - source: config-env
        target: /opt/conf/env.conf
    deploy:
      mode: replicated
      replicas: 1
configs:
  config-env:
    file: ./config/env.conf
